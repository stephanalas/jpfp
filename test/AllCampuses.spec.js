const { expect } = require('chai');
const AllCampuses = require('../client/components/AllCampuses');

describe('AllCampuses', () => {
  let AllCampusesComponent;
  beforeEach(() => {
    AllCampusesComponent = AllCampuses;
    console.log(AllCampusesComponent);
  })
  it('renders "No Campuses" if campuses is an empty array', () => {
    

  }) 
})